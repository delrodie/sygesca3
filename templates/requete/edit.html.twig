{% extends 'backend_layout.html.twig' %}

{% block title %}{{ parent() }} Gestion des requetes{% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="row">
            <div class="col-sm-12">
                <div class="page-title-box">
                    <h4 class="page-title">Liste des requetes</h4>
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-md-12">
                <div class="card">
                    <div class="card-body">
                        {{ include('requete/_form.html.twig', {'button_label': 'Modifier'}) }}

                        {{ include('requete/_delete_form.html.twig') }}
                    </div>
                </div>
            </div> <!-- end col -->
        </div>
    </div>
{% endblock %}


