{% extends 'base.html.twig' %}

{% block title %}Fonctions{% endblock %}

{% block body %}
    <h1>Fonctions</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fonction.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ fonction.libelle }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ fonction.montant }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ fonction.createdAt }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ fonction.updatedAt }}</td>
            </tr>
            <tr>
                <th>Ristourne</th>
                <td>{{ fonction.ristourne }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('fonctions_index') }}">back to list</a>

    <a href="{{ path('fonctions_edit', {'id': fonction.id}) }}">edit</a>

    {{ include('fonctions/_delete_form.html.twig') }}
{% endblock %}
